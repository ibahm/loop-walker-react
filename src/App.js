import React from "react";
import Grid from "./components/Grid";

const App = () => {
  return (
    <div className="container">
      <Grid/>
    </div>
  );
}

setInterval(App, 1000);

export default App;
